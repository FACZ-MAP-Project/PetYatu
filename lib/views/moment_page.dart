import 'package:flutter/material.dart';

class CatMoment extends StatelessWidget {
  const CatMoment({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cats'));
  }
}
